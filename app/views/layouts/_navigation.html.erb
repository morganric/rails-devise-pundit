<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag('embedtree-logo.png', :width => "40", :class => "pull-left"), root_path %>
      <%= link_to raw('EmbedTree<sup> &beta;eta</sup>'), root_path, class: 'navbar-brand' %>
      <div class="nav navbar-nav pull-left">
      <%= form_tag search_path, :method => 'post', :class => "navbar-form navbar-left", :role => "search" do %>
        <div class="form-group">
          <%= text_field_tag :q, params[:q], :class =>"form-control", :placeholder => ""  %>
          <%= submit_tag "Search", :name => nil, :class => "btn btn-default" %>
        </div>
      <% end %>
      </div>
    </div>
    <div class="collapse navbar-collapse pull-right">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
    </div>
  </div>
</nav>
